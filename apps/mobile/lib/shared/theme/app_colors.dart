import '../../core/theme/colors.dart';

class AppThemeColors {
  static const bg0 = AppColors.bg0;
  static const bg1 = AppColors.bg1;
  static const bg2 = AppColors.card;
  static const stroke = AppColors.stroke;
  static const primary = AppColors.accent;
  static const primaryGlow = AppColors.accent2;
  static const textPrimary = AppColors.textPrimary;
  static const textMuted = AppColors.textSecondary;
  static const success = AppColors.success;
  static const danger = AppColors.danger;
}
