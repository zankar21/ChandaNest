{
  "name": "chandanest-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint:web-public": "node scripts/guardrails.mjs --target web-public",
    "lint:web-admin": "node scripts/guardrails.mjs --target web-admin",
    "smoke": "node scripts/guardrails.mjs && npm -C apps/web-public run build && npm -C apps/web-admin run build"
  }
}
